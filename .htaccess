Options -Indexes

# Git: Never serve repository internals, history is readable otherwise
RedirectMatch 404 ^/\.git

# Force HTTPS
RewriteEngine On
RewriteCond %{SERVER_PORT} 80
RewriteRule ^(.*)$ https://%{HTTP_HOST}%{REQUEST_URI} [L,R=301,NE]

# Error pages
ErrorDocument 404 /404.html
