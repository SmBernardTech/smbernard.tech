# Secrets: The live config sits above the web root on the server, not here.
# These lines are a safety net in case a copy ever lands in the site folder.
turnstile-config.php
assets/php/turnstile-config.php

# OS and editor leftovers
.DS_Store
Thumbs.db
desktop.ini
